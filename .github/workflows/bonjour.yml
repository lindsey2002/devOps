name: Testing github workflows

on: push

jobs:
  init:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'hello Heredia'
      - run: echo $HOSTNAME
      - run: echo "GODD JOB ${{ vars.STEP4}}"
      - run: echo " ${{vars.TAIST}}"
